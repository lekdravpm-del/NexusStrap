<local:PickerControlBase x:Class="ColorPicker.SquarePicker"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:local="clr-namespace:ColorPicker"
                         xmlns:controls="clr-namespace:ColorPicker.UserControls"
                         x:Name="uc"
                         MinWidth="150" MinHeight="120">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="24" />
        </Grid.ColumnDefinitions>

        <controls:SquareSlider Grid.Column="0" 
                               RangeX="100" RangeY="100"
                               Hue="{Binding ElementName=uc, Path=Color.HSV_H}"
                               HeadX="{Binding ElementName=uc, Path=Color.HSV_S, Mode=TwoWay}"
                               HeadY="{Binding ElementName=uc, Path=Color.HSV_V, Mode=TwoWay}" />

        <controls:HueSlider Grid.Column="2"
                            SmallChange="{Binding ElementName=uc, Path=SmallChange}"
                            Value="{Binding ElementName=uc, Path=Color.HSV_H, Mode=TwoWay}" />
    </Grid>
</local:PickerControlBase>