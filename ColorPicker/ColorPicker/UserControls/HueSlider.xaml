<UserControl x:Class="ColorPicker.UserControls.HueSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:ClassModifier="internal"
             x:Name="uc">
    <Grid>
        <Border CornerRadius="4" BorderThickness="1" BorderBrush="#3F3F46" ClipToBounds="True">
            <Border.Background>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Color="#FF0000" Offset="0.0" />
                    <GradientStop Color="#FFFF00" Offset="0.166" />
                    <GradientStop Color="#00FF00" Offset="0.333" />
                    <GradientStop Color="#00FFFF" Offset="0.5" />
                    <GradientStop Color="#0000FF" Offset="0.666" />
                    <GradientStop Color="#FF00FF" Offset="0.833" />
                    <GradientStop Color="#FF0000" Offset="1.0" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <Slider Orientation="Vertical" 
                Minimum="0" Maximum="360" 
                Value="{Binding ElementName=uc, Path=Value, Mode=TwoWay}"
                IsDirectionReversed="True"
                IsMoveToPointEnabled="True">
            <Slider.Style>
                <Style TargetType="Slider">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Slider">
                                <Grid Background="Transparent">
                                    <Track x:Name="PART_Track">
                                        <Track.Thumb>
                                            <Thumb Height="8" Width="24">
                                                <Thumb.Template>
                                                    <ControlTemplate>
                                                        <Border Background="White" BorderBrush="#3F3F46" 
                                                                BorderThickness="1" CornerRadius="2">
                                                            <Border.Effect>
                                                                <DropShadowEffect BlurRadius="3" ShadowDepth="0.5" Opacity="0.5"/>
                                                            </Border.Effect>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Thumb.Template>
                                            </Thumb>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Slider.Style>
        </Slider>
    </Grid>
</UserControl>