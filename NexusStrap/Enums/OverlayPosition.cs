namespace NexusStrap.Enums {
    public enum OverlayPosition {
        TopLeft,
        TopRight,
        BottomLeft,
        BottomRight
    }
}

namespace NexusStrap.Enums {
    public enum OverlayStyle {
        Horizontal,
        Vertical,
        Compact,
        Fullscreen
    }
}
