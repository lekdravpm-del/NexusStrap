<base:WpfUiWindow
    x:Class="NexusStrap.UI.Elements.Dialogs.ChannelListsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:NexusStrap.UI.Elements.Base"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NexusStrap.UI.Elements.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:NexusStrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Available Channels"
    Width="700"
    MinHeight="350"
    Height="550"
    Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Background="{DynamicResource ApplicationBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar
            Title="{x:Static resources:Strings.Menu_ChannelChanger_AvailableChannels}"
            Grid.Row="0" 
            Grid.ColumnSpan="2" 
            Padding="8" 
            Icon="pack://application:,,,/NX.ico"
            CanMaximize="True"
            ShowMaximize="True"
            ShowMinimize="True" />

        <TextBlock
            Grid.Row="1"
            Text="{x:Static resources:Strings.Menu_ChannelChanger_AvailableChannels_Description}" 
            FontSize="12"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            Margin="8,8,0,4"
            TextWrapping="Wrap" />

        <Grid Grid.Row="2" Margin="8">
            <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Border Background="{DynamicResource ControlFillColorDefaultBrush}" Opacity="0.7" />
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar Width="200" Height="8" IsIndeterminate="True"/>
                    <TextBlock Text="{x:Static resources:Strings.Common_Loading}" FontSize="13" Margin="0,12,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <DataGrid
                ItemsSource="{Binding Channels}"
                Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                HeadersVisibility="Column"
                CanUserResizeRows="False"
                CanUserResizeColumns="True"
                PreviewKeyDown="ChannelDataGrid_PreviewKeyDown"
                SelectionMode="Single"
                GridLinesVisibility="Horizontal">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
                        <Setter Property="Height" Value="30" />
                        <Setter Property="Padding" Value="8,0,12,0" />
                        <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Height" Value="30" />
                        <Setter Property="Padding" Value="6,0,6,0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Stretch" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#14000000" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#59007ACC" />
                                <Setter Property="Foreground" Value="#F0F0F0" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True" />
                                    <Condition Property="IsMouseOver" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="#8C007ACC" />
                            </MultiTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Channel Name" Binding="{Binding ChannelName}" Width="150" />
                    <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="*" />
                    <DataGridTextColumn Header="Version GUID" Binding="{Binding VersionGuid}" Width="*" />
                    <DataGridTextColumn Header="Deployment Date" Binding="{Binding DisplayTimestamp}" Width="160" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Border Grid.Row="3" Margin="0,10,0,0" Padding="15" Background="{DynamicResource PrimaryBackgroundColor}">
            <StackPanel HorizontalAlignment="Right" FlowDirection="LeftToRight" Orientation="Horizontal">
                <ui:Button MinWidth="100" Content="{x:Static resources:Strings.Common_Refresh}" Command="{Binding RefreshCommand}" Appearance="Primary" Icon="ArrowSync24"  />
                <Button MinWidth="100" Margin="12,0,0,0"  Click="Close_Click" Content="{x:Static resources:Strings.Common_Close}" IsCancel="True" />
            </StackPanel>
        </Border>
    </Grid>
</base:WpfUiWindow>