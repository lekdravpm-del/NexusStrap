<base:WpfUiWindow x:Class="NexusStrap.UI.Elements.Dialogs.AddFriendDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:base="clr-namespace:NexusStrap.UI.Elements.Base"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Add a friend"
        Width="460" Height="380"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Background="{DynamicResource ApplicationBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" Title="Add a friend" Icon="pack://application:,,,/NX.ico" ShowMaximize="False" CanMaximize="False" ShowMinimize="False"/>

        <StackPanel Grid.Row="1" Margin="20,12,20,16">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <ui:SymbolIcon Symbol="PersonAdd24" FontSize="20" Margin="0,0,8,0"/>
                <TextBlock Text="Add a friend by ID" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Text="Paste their Nexus ID (Roblox User ID). They must have logged in via NexusStrap at least once." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,12"/>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:TextBox x:Name="IdBox" PlaceholderText="e.g. 123456789" FontFamily="Consolas" FontSize="13" Height="36" VerticalContentAlignment="Center"/>
                <ui:Button Grid.Column="1" Margin="8,0,0,0" Height="36" Appearance="Primary" Content="Search" Click="Search_Click"/>
            </Grid>

            <Border x:Name="ResultBorder" Visibility="Collapsed" Padding="12" CornerRadius="10" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="AvatarBorder" Width="48" Height="48" CornerRadius="24" Margin="0,0,12,0">
                        <Border.Background>
                            <ImageBrush x:Name="AvatarBrush" Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="NameText" FontSize="14" FontWeight="Bold"/>
                        <TextBlock x:Name="UserText" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="IdText" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" FontFamily="Consolas" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <TextBlock x:Name="StatusText" Margin="0,10,0,0" FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Visibility="Collapsed"/>

            <ui:ProgressRing x:Name="LoadingRing" Width="28" Height="28" IsIndeterminate="True" Visibility="Collapsed" Margin="0,12,0,0" HorizontalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="2" Background="{DynamicResource PrimaryBackgroundColor}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="16,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="1" MinWidth="90" Margin="0,0,8,0" Appearance="Primary" Content="Send" Click="Send_Click" x:Name="SendBtn" IsEnabled="False"/>
                <ui:Button Grid.Column="2" MinWidth="90" Appearance="Secondary" Content="Close" Click="Close_Click"/>
            </Grid>
        </Border>
    </Grid>
</base:WpfUiWindow>