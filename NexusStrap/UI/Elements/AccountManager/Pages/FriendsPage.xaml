<ui:UiPage x:Class="NexusStrap.UI.Elements.AccountManagers.Pages.FriendsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:dmodels="clr-namespace:NexusStrap.UI.ViewModels.AccountManagers"
      xmlns:controls="clr-namespace:NexusStrap.UI.Elements.Controls"
      xmlns:enums="clr-namespace:NexusStrap.Enums"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:resources="clr-namespace:NexusStrap.Resources"
      mc:Ignorable="d"
      d:DesignHeight="500" d:DesignWidth="850"
      Title="Friends Page"
      Scrollable="True">

    <Grid>
        <Grid Margin="8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>


            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock VerticalAlignment="Center" FontSize="18" FontWeight="Bold" Text="Friends" Margin="0,0,12,0" />
                <Border Padding="8,4" CornerRadius="6" VerticalAlignment="Center" Margin="0,0,12,0">
                    <Border.Background>
                        <SolidColorBrush Color="{DynamicResource SystemAccentColor}" Opacity="0.12"/>
                    </Border.Background>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Filter" Margin="0,0,8,0" />
                        <ComboBox Width="110" ItemsSource="{Binding FriendFilters}" SelectedItem="{Binding SelectedFriendFilter}"/>
                    </StackPanel>
                </Border>
                <ui:Button Margin="4,0,0,0" Command="{Binding RefreshFriendsCommand}" Content="Refresh" />
            </StackPanel>

            <ItemsControl ItemsSource="{Binding FilteredFriends}" Grid.Row="2" HorizontalAlignment="Stretch">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid HorizontalAlignment="Stretch" VerticalAlignment="Top" Columns="7"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,4,4,4"
                                Padding="10"
                                Background="{DynamicResource PrimaryBackgroundColor}"
                                CornerRadius="10"
                                BorderThickness="1,0,1,1">
                            <Border.BorderBrush>
                                <SolidColorBrush Color="{DynamicResource SystemAccentColor}" Opacity="0.15"/>
                            </Border.BorderBrush>

                            <StackPanel>
                                <Grid>
                                    <Border
                                        Width="56"
                                        Height="56"
                                        CornerRadius="10">
                                        <Border.Background>
                                            <ImageBrush ImageSource="{Binding AvatarUrl, TargetNullValue={x:Null}, FallbackValue={x:Null}}" Stretch="UniformToFill" />
                                        </Border.Background>
                                    </Border>
                                    <Border
                                        Width="16"
                                        Height="16"
                                        Margin="0,0,16,0"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Bottom"
                                        Background="{Binding StatusColor}"
                                        BorderBrush="{DynamicResource ApplicationBackgroundBrush}"
                                        BorderThickness="2"
                                        CornerRadius="8"
                                        ToolTip="{Binding LastLocation}">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="4" Opacity="0.6" ShadowDepth="0" />
                                        </Border.Effect>
                                    </Border>
                                </Grid>

                                <TextBlock
                                    Margin="0,6,0,0"
                                    FontSize="10"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="{Binding LastLocation}"
                                    ToolTip="{Binding LastLocation}"
                                    TextTrimming="CharacterEllipsis" />

                                <ui:Button
                                    Margin="0,8,0,0"
                                    HorizontalAlignment="Stretch"
                                    Appearance="Primary"
                                    Command="{Binding DataContext.JoinFriendCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Height="30"
                                    IsEnabled="{Binding IsOnline}">
                                    <ui:SymbolIcon Filled="True" Symbol="Play24" FontSize="16" Foreground="White" />
                                </ui:Button>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</ui:UiPage>