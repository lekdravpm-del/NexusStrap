<ui:UiWindow x:Class="NexusStrap.UI.Elements.Overlay.PerformanceOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="PerformanceOverlay"
    Width="220"
    Height="140"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    Topmost="True"
    ShowInTaskbar="False"
    ResizeMode="NoResize"
    WindowStartupLocation="Manual"
    Left="20"
    Top="20">

    <Border x:Name="OverlayBorder"
        Background="#CC0D0D0D"
        CornerRadius="8"
        Padding="10,8"
        BorderBrush="#33FFFFFF"
        BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.5" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="NEXUS STRAP" FontSize="9" FontWeight="Bold" Foreground="#AAAAAA" Margin="0,0,0,4" />

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,1,0,0">
                <TextBlock Text="CPU: " FontSize="11" Foreground="#888888" />
                <TextBlock x:Name="CpuText" Text="0%" FontSize="11" FontWeight="SemiBold" Foreground="White" />
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,1,0,0">
                <TextBlock Text="MEM: " FontSize="11" Foreground="#888888" />
                <TextBlock x:Name="MemText" Text="0 MB" FontSize="11" FontWeight="SemiBold" Foreground="White" />
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,1,0,0">
                <TextBlock Text="FPS: " FontSize="11" Foreground="#888888" />
                <TextBlock x:Name="FpsText" Text="--" FontSize="11" FontWeight="SemiBold" Foreground="#4ADE80" />
            </StackPanel>

            <TextBlock Grid.Row="4" x:Name="StatusText" Text="" FontSize="9" Foreground="#666666" Margin="0,3,0,0" />
        </Grid>
    </Border>
</ui:UiWindow>
