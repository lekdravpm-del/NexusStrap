<base:WpfUiWindow
    x:Class="NexusStrap.UI.Elements.ContextMenu.ServerHistory"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:NexusStrap.UI.Elements.Base"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:enums="clr-namespace:NexusStrap.Enums"
    xmlns:local="clr-namespace:NexusStrap.UI.Elements.ContextMenu"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:NexusStrap.UI.ViewModels.ContextMenu"
    xmlns:resources="clr-namespace:NexusStrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{x:Static resources:Strings.ContextMenu_GameHistory_Title}"
    Width="580"
    Height="420"
    MinWidth="420"
    MinHeight="420"
    d:DataContext="{d:DesignInstance Type=models:ServerHistoryViewModel}"
    Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    ExtendsContentIntoTitleBar="True"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    
    <Grid Background="{DynamicResource ApplicationBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            x:Name="RootTitleBar"
            Title="{x:Static resources:Strings.ContextMenu_GameHistory_Title}"
            Grid.Row="0"
            Grid.ColumnSpan="2"
            Padding="8"
            CanMaximize="False"
            Icon="pack://application:,,,/NX.ico"
            KeyboardNavigation.TabNavigation="None"
            ShowMaximize="False"
            ShowMinimize="False" />

        <TextBlock
            Grid.Row="1"
            Margin="16,8,16,0"
            Text="{x:Static resources:Strings.ContextMenu_GameHistory_Description}"
            TextWrapping="Wrap" />

        <TextBlock
            Grid.Row="2"
            Margin="16,8,16,0"
            Text="{Binding Error, Mode=OneWay}"
            TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding LoadState, Mode=OneWay}" Value="{x:Static enums:GenericTriState.Failed}">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                    <Setter Property="Visibility" Value="Collapsed" />
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Border Grid.Row="2">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding LoadState, Mode=OneWay}" Value="{x:Static enums:GenericTriState.Unknown}">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                    <Setter Property="Visibility" Value="Collapsed" />
                </Style>
            </Border.Style>

            <ui:ProgressRing Grid.Row="1" IsIndeterminate="True" />
        </Border>

        <ListView
            Grid.Row="2"
            Margin="8"
            ItemsSource="{Binding GameHistory, Mode=OneWay}">
            <ListView.Style>
                <Style BasedOn="{StaticResource {x:Type ListView}}" TargetType="ListView">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding LoadState, Mode=OneWay}" Value="{x:Static enums:GenericTriState.Successful}">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                    <Setter Property="Visibility" Value="Collapsed" />
                </Style>
            </ListView.Style>
            <ListView.ItemContainerStyle>
                <Style BasedOn="{StaticResource {x:Type ListViewItem}}" TargetType="{x:Type ListViewItem}">
                    <Setter Property="Focusable" Value="False" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ui:Card Padding="0">
                        <Grid VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border
                                Grid.Column="0"
                                Width="107"
                                Height="107"
                                CornerRadius="4">
                                <Border.Background>
                                    <ImageBrush ImageSource="{Binding UniverseDetails.Thumbnail.ImageUrl, IsAsync=True}" />
                                </Border.Background>
                            </Border>

                            <StackPanel
                                Grid.Column="1"
                                Margin="16,12,12,12"
                                VerticalAlignment="Center">
                                <TextBlock
                                    FontSize="18"
                                    FontWeight="Medium"
                                    Text="{Binding UniverseDetails.Data.Name}" />
                                <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding JobId}" />

                                <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
                                    <ui:Button
                                        Appearance="Success"
                                        Command="{Binding RejoinServerCommand}"
                                        Content="{x:Static resources:Strings.ContextMenu_GameHistory_Rejoin}"
                                        Foreground="White"
                                        Icon="Play28"
                                        IconFilled="True"
                                        IconForeground="White" />

                                    <ui:Button
                                        Margin="8,0,0,0"
                                        Appearance="Secondary"
                                        Command="{Binding CopyDeeplinkCommand}"
                                        Content="DeepLink"
                                        Icon="Copy24" />

                                    <ui:Button
                                        Margin="8,0,0,0"
                                        Appearance="Secondary"
                                        Command="{Binding CopyServerIdCommand}"
                                        Content="Server ID"
                                        Icon="Clipboard24" />

                                    <ui:Button
                                        Margin="8,0,0,0"
                                        Appearance="Danger"
                                        Command="{Binding DeleteHistoryCommand}"
                                        Content="{x:Static resources:Strings.Common_Delete}"
                                        Icon="Delete24"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ui:Card>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</base:WpfUiWindow>