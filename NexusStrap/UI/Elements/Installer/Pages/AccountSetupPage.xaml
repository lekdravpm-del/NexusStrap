<ui:UiPage x:Class="NexusStrap.UI.Elements.Installer.Pages.AccountSetupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:resources="clr-namespace:NexusStrap.Resources"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="600"
      Title="AccountSetup"
      Scrollable="True"
      Loaded="UiPage_Loaded"
      Background="Transparent">

    <Grid Margin="20,12,20,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,16">
            <ui:SymbolIcon Symbol="PersonAccounts24" FontSize="32" HorizontalAlignment="Center" Foreground="{DynamicResource SystemAccentBrush}"/>
            <TextBlock Text="Link your Roblox account" FontSize="19" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,8,0,4"/>
            <TextBlock Text="Your Roblox ID is your friend code. It never changes for the same account." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextAlignment="Center" MaxWidth="420" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Not logged in -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="420">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoggedIn}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>

            <Border Padding="14" CornerRadius="10" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="How it works" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6"/>
                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Text="• Log in with your Roblox account&#x0a;• We save your User ID (e.g. 123456789)&#x0a;• Same account = same ID forever&#x0a;• Share your ID with friends to add each other"/>
                </StackPanel>
            </Border>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" Margin="0,0,8,0" Height="36" VerticalContentAlignment="Center" ItemsSource="{Binding AddMethods}" SelectedItem="{Binding SelectedAddMethod}"/>
                <ui:Button Grid.Column="1" Height="36" Appearance="Primary" Command="{Binding LoginCommand}" Content="Log In" Padding="22,0" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
            </Grid>
            <TextBlock Text="Installing Chromium… this may take a while" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,8">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsInstallingChromium}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ui:Button HorizontalAlignment="Center" Appearance="Secondary" Content="Skip for now" Click="Skip_Click" Padding="16,6" Margin="0,8,0,0"/>
            <TextBlock Text="You can link or switch accounts anytime in Settings → Account Manager" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,8,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Logged in -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="440">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoggedIn}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>

            <Border Padding="16" CornerRadius="12" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource SystemAccentBrush}" BorderThickness="1.2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Width="64" Height="64" CornerRadius="32" Margin="0,0,14,0" VerticalAlignment="Center">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding AvatarUrl}" Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding DisplayName}" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Username}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,1,0,8"/>
                        <Border Padding="8,6" CornerRadius="8" Background="{DynamicResource ControlFillColorDefaultBrush}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="ID" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Grid.Column="1" Text="{Binding UserIdText}" FontSize="13" FontWeight="SemiBold" FontFamily="Consolas" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <ui:Button Grid.Column="2" Width="28" Height="28" Padding="0" Appearance="Secondary" Command="{Binding CopyIdCommand}" ToolTip="Copy ID" Margin="8,0,0,0">
                                    <ui:SymbolIcon Symbol="Copy24" FontSize="14"/>
                                </ui:Button>
                            </Grid>
                        </Border>
                        <TextBlock Text="This ID is your Roblox User ID. Same account = same ID forever." FontSize="10" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,6,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,14,0,0">
                <ui:Button Appearance="Primary" Content="Continue" Click="Continue_Click" Padding="22,0" Height="36" Margin="0,0,8,0"/>
                <ui:Button Appearance="Secondary" Content="Switch account" Command="{Binding LoginCommand}" Padding="14,0" Height="36"/>
            </StackPanel>
            <ui:Button HorizontalAlignment="Center" Appearance="Transparent" Content="Skip" Click="Skip_Click" Margin="0,8,0,0" FontSize="11"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
            <ui:SymbolIcon Symbol="Info24" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,0,6,0"/>
            <TextBlock Text="Friend system uses this ID to find you" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
        </StackPanel>
    </Grid>
</ui:UiPage>