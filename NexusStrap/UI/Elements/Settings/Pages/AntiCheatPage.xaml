<ui:UiPage x:Class="NexusStrap.UI.Elements.Settings.Pages.AntiCheatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Anti-Cheat Awareness"
    Scrollable="True"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800">
    <StackPanel Margin="0,8,14,14">
        <TextBlock FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Scans your active FFlags against a database of known anti-cheat triggers. Flags marked High severity are most likely to cause issues with Byfron or other anti-cheat systems." Margin="0,0,0,4" />
        <TextBlock FontSize="20" FontWeight="Bold" Text="Anti-Cheat Awareness" Margin="0,0,0,12" />

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="0" Content="Scan Flags" Appearance="Primary" Command="{Binding RefreshCommand}" Margin="0,0,12,0" />
            <TextBlock Grid.Column="1" Text="{Binding StatusText}" VerticalAlignment="Center" FontSize="14" FontWeight="Medium" />
        </Grid>

        <Border Padding="12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock FontSize="14" FontWeight="SemiBold" Text="Warnings" Foreground="#FFB44444" Margin="0,0,0,8" />
                <ItemsControl ItemsSource="{Binding WarningMessages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="No warnings found." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            </StackPanel>
        </Border>

        <Border Padding="12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8">
            <StackPanel>
                <TextBlock FontSize="14" FontWeight="SemiBold" Text="Safe Flags" Foreground="#FF44BB44" Margin="0,0,0,8" />
                <ItemsControl ItemsSource="{Binding SafeMessages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="Run a scan to see safe flags." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            </StackPanel>
        </Border>
    </StackPanel>
</ui:UiPage>
