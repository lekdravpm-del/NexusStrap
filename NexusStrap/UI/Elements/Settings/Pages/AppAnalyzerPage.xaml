<ui:UiPage x:Class="NexusStrap.UI.Elements.Settings.Pages.AppAnalyzerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="App Analyzer"
    Scrollable="True"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800">
    <StackPanel Margin="0,8,14,14">
        <TextBlock FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="All-in-one diagnostics: anti-cheat flag scan, flag conflict detection and environment health check." Margin="0,0,0,4" />
        <TextBlock FontSize="20" FontWeight="Bold" Text="App Analyzer" Margin="0,0,0,12" />

        <TabControl>
            <!-- Anti-Cheat Scan -->
            <TabItem Header="Anti-Cheat Scan">
                <StackPanel Margin="0,12,0,0">
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:Button Grid.Column="0" Content="Scan Flags" Appearance="Primary" Command="{Binding ScanAntiCheatCommand}" Margin="0,0,12,0" />
                        <TextBlock Grid.Column="1" Text="{Binding AntiCheatStatusText}" VerticalAlignment="Center" FontSize="13" FontWeight="Medium" TextWrapping="Wrap" />
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Padding="12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8">
                            <StackPanel>
                                <TextBlock FontSize="14" FontWeight="SemiBold" Text="Warnings" Foreground="#FFB44444" Margin="0,0,0,8" />
                                <ItemsControl ItemsSource="{Binding WarningMessages}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Text="No warnings found." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Padding="12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8">
                            <StackPanel>
                                <TextBlock FontSize="14" FontWeight="SemiBold" Text="Safe Flags" Foreground="#FF44BB44" Margin="0,0,0,8" />
                                <ItemsControl ItemsSource="{Binding SafeMessages}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Text="Run a scan to see safe flags." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </TabItem>

            <!-- Conflict Detector -->
            <TabItem Header="Conflict Detector">
                <StackPanel Margin="0,12,0,0">
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:Button x:Name="ScanConflictsButton" Grid.Column="0" Content="Scan for Conflicts" Appearance="Primary" Command="{Binding ScanConflictsCommand}" Margin="0,0,12,0" />
                        <TextBlock Grid.Column="1" Text="{Binding ConflictStatusText}" VerticalAlignment="Center" FontSize="13" FontWeight="Medium" TextWrapping="Wrap" />
                    </Grid>

                    <Border Padding="12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8">
                        <StackPanel>
                            <TextBlock FontSize="14" FontWeight="SemiBold" Text="Detected Conflicts" Margin="0,0,0,8" />
                            <ItemsControl ItemsSource="{Binding Conflicts}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Text="No conflicts detected. Your flags look clean!" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </TabItem>

            <!-- Health Check -->
            <TabItem Header="Health Check">
                <StackPanel Margin="0,12,0,0">
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:Button x:Name="RunHealthCheckButton" Grid.Column="0" Content="Run Health Check" Appearance="Primary" Command="{Binding RunHealthCheckCommand}" Margin="0,0,12,0" />
                        <TextBlock Grid.Column="1" Text="Diagnoses common issues that may prevent Roblox from launching." VerticalAlignment="Center" FontSize="13" FontWeight="Medium" />
                    </Grid>

                    <Border Padding="12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8">
                        <ScrollViewer MaxHeight="380">
                            <TextBlock
                                Text="{Binding CheckResults}"
                                FontFamily="Consolas"
                                FontSize="12"
                                TextWrapping="Wrap"
                                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </TabItem>
        </TabControl>
    </StackPanel>
</ui:UiPage>