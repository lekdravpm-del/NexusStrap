<ui:UiPage
    x:Class="NexusStrap.UI.Elements.Settings.Pages.RobloxSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:NexusStrap.UI.Elements.Controls"
    xmlns:converters="clr-namespace:NexusStrap.UI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:NexusStrap.Models"
    xmlns:resources="clr-namespace:NexusStrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="RobloxSettingsPage"
    d:DesignHeight="1500"
    d:DesignWidth="800"
    Scrollable="True"
    mc:Ignorable="d">

    <ui:UiPage.Resources>
        <DataTemplate x:Key="TextBoxTemplate">
            <ui:TextBox
                Margin="0,0,5,4"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </DataTemplate>

        <DataTemplate x:Key="SliderTemplate">
            <Grid Margin="0,0,0,4" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Slider
                    Grid.Column="0"
                    IsSnapToTickEnabled="True"
                    AutoToolTipPlacement="TopLeft" 
                    AutoToolTipPrecision="1"
                    Maximum="{Binding MaxValue}"
                    Minimum="{Binding MinValue}"
                    TickFrequency="{Binding Step}"
                    Value="{Binding TypedValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock
                    Grid.Column="1"
                    Width="25"
                    Margin="6,0,0,0"
                    VerticalAlignment="Center"
                    Text="{Binding Value}"
                    TextAlignment="Right" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="ToggleSwitchTemplate">
            <ui:ToggleSwitch 
                Margin="0,0,0,4"
                VerticalAlignment="Center" 
                IsChecked="{Binding TypedValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </DataTemplate>

        <DataTemplate x:Key="ComboBoxTemplate">
            <ComboBox
                Margin="0,0,5,4"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                DisplayMemberPath="DisplayName"
                SelectedValuePath="Value"
                ItemsSource="{Binding Options}"
                SelectedValue="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </DataTemplate>

        <DataTemplate x:Key="Vector2Template">
            <Grid Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:TextBox
                    Grid.Column="0"
                    Margin="0,0,4,0"
                    ClearButtonEnabled="False"
                    PlaceholderText="X"
                    Text="{Binding VectorX, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <ui:TextBox
                    Grid.Column="1"
                    Margin="4,0,0,0"
                    ClearButtonEnabled="False"
                    PlaceholderText="Y"
                    Text="{Binding VectorY, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
        </DataTemplate>

        <Style x:Key="SettingsContentStyle" TargetType="ContentPresenter">
            <Setter Property="ContentTemplate" Value="{StaticResource TextBoxTemplate}" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Type}" Value="Slider">
                    <Setter Property="ContentTemplate" Value="{StaticResource SliderTemplate}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="ToggleSwitch">
                    <Setter Property="ContentTemplate" Value="{StaticResource ToggleSwitchTemplate}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="ComboBox">
                    <Setter Property="ContentTemplate" Value="{StaticResource ComboBoxTemplate}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="Vector2">
                    <Setter Property="ContentTemplate" Value="{StaticResource Vector2Template}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </ui:UiPage.Resources>

    <StackPanel Margin="0,0,14,14">
        <TextBlock
            Margin="0,0,0,16"
            FontSize="14"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            Text="{x:Static resources:Strings.Menu_GBSEditor_Description}" />

        <Grid Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:CardAction Grid.Column="0" Margin="0,0,4,0" Command="{Binding ImportCommand}" Icon="FolderAdd24">
                <TextBlock FontSize="14" Text="Import Settings" />
            </ui:CardAction>
            <ui:CardAction Grid.Column="1" Margin="4,0,4,0" Command="{Binding OpenRobloxFolderCommand}" Icon="Folder24">
                <TextBlock FontSize="14" Text="Open Folder" />
            </ui:CardAction>
            <ui:CardAction Grid.Column="2" Margin="4,0,0,0" Command="{Binding ExportCommand}" Icon="Save24">
                <TextBlock FontSize="14" Text="Export Settings" />
            </ui:CardAction>
        </Grid>

        <controls:OptionControl Description="{x:Static resources:Strings.Menu_GBSEditor_ReadOnly_Description}" Header="{x:Static resources:Strings.Menu_GBSEditor_ReadOnly_Title}">
            <ui:ToggleSwitch Margin="5,0,0,0" IsChecked="{Binding ReadOnly}" />
        </controls:OptionControl>

        <ItemsControl ItemsSource="{Binding Sections}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,8,0,0">
                        <Separator Margin="0,8,0,4" />
                        <TextBlock FontSize="20" FontWeight="Bold" Text="{Binding Title}" />
                        <ItemsControl ItemsSource="{Binding Controls}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="4" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <controls:SquareCard Description="{Binding Description}" Header="{Binding Title}">
                                        <ContentPresenter Content="{Binding}" Style="{StaticResource SettingsContentStyle}" />
                                    </controls:SquareCard>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</ui:UiPage>