<ui:UiPage
    x:Class="NexusStrap.UI.Elements.Settings.Pages.ShortcutsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:NexusStrap.UI.Elements.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dmodels="clr-namespace:NexusStrap.UI.ViewModels.Settings"
    xmlns:local="clr-namespace:NexusStrap.UI.Elements.Settings.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:NexusStrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="ShortcutsPage"
    d:DataContext="{d:DesignInstance dmodels:ShortcutsViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="800"
    d:DesignWidth="800"
    Scrollable="True"
    mc:Ignorable="d">

    <StackPanel Margin="0,0,14,14">
        <TextBlock
            Margin="0,0,0,16"
            FontSize="14"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            Text="{x:Static resources:Strings.Menu_Shortcuts_Description}" />

        <controls:OptionControl x:Name="ExtractIconsToggle"
            Description="{Binding Source={x:Static resources:Strings.Menu_Shortcuts_ExtractIcons_Description}, Converter={StaticResource StringFormatConverter}, ConverterParameter='https://github.com/lekdravpm-del/NexusStrap/wiki/Using-Roblox-icons-for-your-shortcuts'}"
            Header="{x:Static resources:Strings.Menu_Shortcuts_ExtractIcons_Title}"
            HelpLink="https://github.com/lekdravpm-del/NexusStrap/wiki/Using-Roblox-icons-for-your-shortcuts">
            <ui:ToggleSwitch IsChecked="{Binding ExtractIconsTask.NewState, Mode=TwoWay}" />
        </controls:OptionControl>

        <Separator Margin="0,6,0,4" />

        <TextBlock
            FontSize="20"
            FontWeight="Bold"
            Text="{x:Static resources:Strings.Menu_Shortcuts_General_Title}" />
        <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Menu_Shortcuts_General_Description}" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <controls:OptionControl x:Name="DesktopIconToggle"
                Grid.Column="0"
                Margin="0,0,4,0"
                Header="{x:Static resources:Strings.Common_Shortcuts_Desktop}">
                <ui:ToggleSwitch IsChecked="{Binding DesktopIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>

            <controls:OptionControl x:Name="StartMenuIconToggle"
                Grid.Column="1"
                Margin="4,0,0,0"
                Header="{x:Static resources:Strings.Common_Shortcuts_StartMenu}">
                <ui:ToggleSwitch IsChecked="{Binding StartMenuIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>
        </Grid>

        <Separator Margin="0,8,0,4" />

        <TextBlock
            FontSize="20"
            FontWeight="Bold"
            Text="{x:Static resources:Strings.Menu_Shortcuts_Function_Title}" />
        <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Menu_Shortcuts_Function_Description}" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <controls:OptionControl x:Name="PlayerIconToggle"
                Grid.Column="0"
                Margin="0,0,2,0"
                Header="{x:Static resources:Strings.LaunchMenu_LaunchRoblox}">
                <ui:ToggleSwitch IsChecked="{Binding PlayerIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>

            <controls:OptionControl x:Name="StudioIconToggle"
                Grid.Column="1"
                Margin="2,0,2,0"
                Header="{x:Static resources:Strings.LaunchMenu_LaunchRobloxStudio}">
                <ui:ToggleSwitch IsChecked="{Binding StudioIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>

            <controls:OptionControl x:Name="SettingsIconToggle"
                Grid.Column="2"
                Margin="2,0,0,0"
                Header="{x:Static resources:Strings.Menu_Title}">
                <ui:ToggleSwitch IsChecked="{Binding SettingsIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>
        </Grid>

        <Separator Margin="0,8,0,4" />

        <TextBlock
            FontSize="20"
            FontWeight="Bold"
            Text="{x:Static resources:Strings.Common_Miscellaneous}" />

        <ui:CardExpander Margin="0,8,0,0" IsExpanded="True">
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock FontSize="14" Text="{x:Static resources:Strings.Menu_Shortcut_GameShortcut_Title}" />
                        <TextBlock FontSize="12" Text="{x:Static resources:Strings.Menu_Shortcut_GameShortcut_Description}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </StackPanel>
                </Grid>
            </ui:CardExpander.Header>

            <StackPanel>
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <Border
                Width="128"
                Height="128"
                VerticalAlignment="Center"
                Background="#33000000"
                CornerRadius="24">
                        <Image
                    Width="128"
                    Height="128"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Source="{Binding SelectedShortcut.IconPath}"
                    Stretch="Uniform"
                    RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>
                    <StackPanel Margin="12,0,0,0" VerticalAlignment="Top">
                        <TextBlock
                    FontSize="18"
                    FontWeight="SemiBold"
                    Text="{Binding SelectedShortcut.GameName}" />
                        <TextBlock FontSize="14" Text="{Binding SelectedShortcut.GameId}" />
                    </StackPanel>
                </StackPanel>

                <TextBlock
            Margin="0,24,0,4"
            FontSize="14"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            Text="Search using game name" />

                <ComboBox
            Height="36"
            IsEditable="True"
            ItemsSource="{Binding SearchResults}"
            SelectedItem="{Binding SelectedSearchResult}"
            Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
            StaysOpenOnEdit="True"
            TextSearch.TextPath="RootPlaceId">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2,0,2,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                            VerticalAlignment="Center"
                            FontWeight="Normal"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Text="{Binding Name}"
                            TextTrimming="CharacterEllipsis" />

                                <StackPanel
                            Grid.Column="1"
                            Margin="8,0,0,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Orientation="Horizontal">
                                    <ui:SymbolIcon Margin="0,0,6,0" VerticalAlignment="Center" Filled="True" FontSize="18" Symbol="People24" />
                                    <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding PlayerCount, Converter={StaticResource NumberAbbreviationConverter}}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <ui:Button
            Margin="0,16,0,0"
            Height="34"
            HorizontalAlignment="Left"
            Command="{Binding CreateShortcutCommand}"
            Content="{x:Static resources:Strings.Menu_Shortcut_GameShortcut_Create}"
            Icon="Shortpick24" />

                <TextBlock
            Margin="0,12,0,0"
            FontWeight="SemiBold"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            Text="{Binding GameShortcutStatus}"
            TextWrapping="Wrap" />
            </StackPanel>
        </ui:CardExpander>
    </StackPanel>
</ui:UiPage>