<ui:UiPage x:Class="NexusStrap.UI.Elements.Settings.Pages.ModConflictPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Mod Conflict Detector"
    Scrollable="True"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800">
    <StackPanel Margin="0,8,14,14">
        <TextBlock FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Detects when two or more FFlags are set to conflicting values that may cause unexpected behavior or crashes." Margin="0,0,0,4" />
        <TextBlock FontSize="20" FontWeight="Bold" Text="Mod Conflict Detector" Margin="0,0,0,12" />

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="0" Content="Scan for Conflicts" Appearance="Primary" Command="{Binding ScanCommand}" Margin="0,0,12,0" />
            <TextBlock Grid.Column="1" Text="{Binding StatusText}" VerticalAlignment="Center" FontSize="14" FontWeight="Medium" />
        </Grid>

        <Border Padding="12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8">
            <StackPanel>
                <TextBlock FontSize="14" FontWeight="SemiBold" Text="Detected Conflicts" Margin="0,0,0,8" />
                <ItemsControl ItemsSource="{Binding Conflicts}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="No conflicts detected. Your flags look clean!" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            </StackPanel>
        </Border>
    </StackPanel>
</ui:UiPage>
