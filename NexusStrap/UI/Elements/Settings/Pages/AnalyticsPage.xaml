<ui:UiPage x:Class="NexusStrap.UI.Elements.Settings.Pages.AnalyticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Analytics Dashboard"
    Scrollable="True"
    mc:Ignorable="d"
    d:DesignHeight="700" d:DesignWidth="800">
    <StackPanel Margin="0,8,14,14">
        <TextBlock FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Overview of your Roblox play sessions and statistics." Margin="0,0,0,4" />
        <TextBlock FontSize="20" FontWeight="Bold" Text="Analytics Dashboard" Margin="0,0,0,12" />

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Padding="16,12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="TOTAL HOURS" />
                    <TextBlock FontSize="22" FontWeight="Bold" Text="{Binding TotalHours}" />
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Padding="16,12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="SESSIONS" />
                    <TextBlock FontSize="22" FontWeight="Bold" Text="{Binding TotalSessions}" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Padding="16,12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="UNIQUE GAMES" />
                    <TextBlock FontSize="22" FontWeight="Bold" Text="{Binding UniqueGamesPlayed}" />
                </StackPanel>
            </Border>
            <Border Grid.Column="3" Padding="16,12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8">
                <StackPanel>
                    <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="MOST PLAYED" />
                    <TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding MostPlayedGame}" TextTrimming="CharacterEllipsis" MaxWidth="150" />
                </StackPanel>
            </Border>
        </Grid>

        <TextBlock FontSize="16" FontWeight="SemiBold" Text="By Game" Margin="0,0,0,8" />
        <Border Padding="12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8" Margin="0,0,0,16">
            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None" BorderThickness="0" RowBackground="Transparent" AlternatingRowBackground="Transparent" CanUserAddRows="False" ItemsSource="{Binding GameStats}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Game" Binding="{Binding GameName}" Width="200" />
                    <DataGridTextColumn Header="Hours" Binding="{Binding TotalMinutes, StringFormat={}{0:F1}}" Width="80" />
                    <DataGridTextColumn Header="Sessions" Binding="{Binding SessionCount}" Width="80" />
                    <DataGridTextColumn Header="Last Played" Binding="{Binding LastPlayed, StringFormat={}{0:MMM dd HH:mm}}" Width="140" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock FontSize="16" FontWeight="SemiBold" Text="Daily Activity" Margin="0,0,0,8" />
        <Border Padding="12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8">
            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None" BorderThickness="0" RowBackground="Transparent" AlternatingRowBackground="Transparent" CanUserAddRows="False" ItemsSource="{Binding DailyStats}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat={}{0:MMM dd, yyyy}}" Width="140" />
                    <DataGridTextColumn Header="Hours" Binding="{Binding TotalMinutes, StringFormat={}{0:F1}}" Width="80" />
                    <DataGridTextColumn Header="Sessions" Binding="{Binding SessionsCount}" Width="80" />
                    <DataGridTextColumn Header="Most Played" Binding="{Binding MostPlayedGame}" Width="200" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <ui:Button x:Name="RefreshButton" Content="Refresh" Command="{Binding RefreshCommand}" HorizontalAlignment="Left" Margin="0,12,0,0" />
    </StackPanel>
</ui:UiPage>
