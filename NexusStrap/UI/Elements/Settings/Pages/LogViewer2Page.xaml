<ui:UiPage x:Class="NexusStrap.UI.Elements.Settings.Pages.LogViewer2Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Log Viewer"
    Scrollable="True"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800">
    <TabControl>
        <TabItem Header="Log Viewer">
            <Grid Margin="0,8,14,14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" FontSize="20" FontWeight="Bold" Text="Log Viewer" Margin="0,0,0,8" />

                <Grid Grid.Row="1" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="LogFilesComboBox" ItemsSource="{Binding LogFiles}" SelectedItem="{Binding SelectedLogFile}" Margin="0,0,8,0" DisplayMemberPath="." />
                    <ui:Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshCommand}" />
                </Grid>

                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <ui:TextBox x:Name="LogSearchTextBox" Grid.Row="0" Margin="0,0,0,8" PlaceholderText="Search logs..." Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <TextBlock Text="{Binding LogContent}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" />
                    </ScrollViewer>
                </Grid>
            </Grid>
        </TabItem>
        <TabItem Header="Launch Arguments">
            <StackPanel Margin="0,8,14,14">
                <TextBlock FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Add custom command-line arguments that will be passed to the Roblox executable on launch." Margin="0,0,0,4" />
                <TextBlock FontSize="20" FontWeight="Bold" Text="Custom Launch Arguments" Margin="0,0,0,12" />

                <Border Padding="16" Background="{DynamicResource PrimaryBackgroundColor}" BorderBrush="{DynamicResource ControlFillColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="Add New Argument" Margin="0,0,0,8" />
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <ui:TextBox Grid.Column="1" Text="{Binding NewArgName, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="e.g., No Splash" />
                        </Grid>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Argument:" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <ui:TextBox Grid.Column="1" Text="{Binding NewArgValue, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="e.g., --no-splash" />
                        </Grid>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Description:" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <ui:TextBox Grid.Column="1" Text="{Binding NewArgDescription, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="What does this flag do?" />
                        </Grid>
                        <ui:Button x:Name="AddArgButton" Content="Add Argument" Appearance="Primary" Command="{Binding AddArgCommand}" HorizontalAlignment="Left" />
                    </StackPanel>
                </Border>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" FontSize="14" FontWeight="SemiBold" Text="Saved Arguments" VerticalAlignment="Center" />
                    <ui:Button Grid.Column="1" Content="Remove Selected" Command="{Binding RemoveArgCommand}" />
                </Grid>

                <ItemsControl ItemsSource="{Binding LaunchArgs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12" Background="{DynamicResource PrimaryBackgroundColor}" BorderBrush="{DynamicResource ControlFillColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Row="0" Grid.Column="0">
                                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding Name}" />
                                        <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding Description}" TextWrapping="Wrap" />
                                    </StackPanel>
                                    <ui:ToggleSwitch Grid.Row="0" Grid.Column="1" IsChecked="{Binding Enabled}" />
                                    <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="{Binding Argument}" Margin="0,4,0,0" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </TabItem>
    </TabControl>
</ui:UiPage>
