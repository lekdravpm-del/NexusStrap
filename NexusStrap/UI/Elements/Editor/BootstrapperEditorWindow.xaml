<base:WpfUiWindow
    x:Class="NexusStrap.UI.Elements.Editor.BootstrapperEditorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:base="clr-namespace:NexusStrap.UI.Elements.Base"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dmodels="clr-namespace:NexusStrap.UI.ViewModels.Editor"
    xmlns:local="clr-namespace:NexusStrap.UI.Elements.Editor"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:NexusStrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{Binding Path=Title, Mode=OneTime}"
    Width="1000"
    Height="500"
    d:DataContext="{d:DesignInstance dmodels:BootstrapperEditorWindowViewModel, IsDesignTimeCreatable=True}"
    Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    Closing="OnClosing"
    ExtendsContentIntoTitleBar="True"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Background="{DynamicResource ApplicationBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            x:Name="RootTitleBar"
            Title="{Binding Path=Title, Mode=OneTime}"
            Grid.Row="0"
            Padding="8"
            ForceShutdown="False"
            Icon="pack://application:,,,/NX.ico"
            MinimizeToTray="False"
            UseSnapLayout="True" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <avalonedit:TextEditor
        x:Name="UIXML"
        Grid.Column="0"
        Margin="10,10,10,10"
        ShowLineNumbers="True"
        Style="{StaticResource NewTextEditor}"
        SyntaxHighlighting="XML" />
        </Grid>
        
        <Grid Grid.Row="2" Margin="10,0,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:Button
        Grid.Column="0"
        HorizontalAlignment="Left"
        Command="{Binding Path=OpenThemeFolderCommand, Mode=OneTime}"
        Content="{x:Static resources:Strings.CustomTheme_Editor_OpenThemeDirectory}" />

            <ui:Button
        Grid.Column="1"
        Margin="10,0,0,0"
        Appearance="Primary"
        Command="{Binding Path=PreviewCommand, Mode=OneTime}"
        Content="{x:Static resources:Strings.CustomTheme_Editor_Preview}" />

            <ui:Button
        Grid.Column="2"
        Margin="10,0,0,0"
        MinWidth="56.587"
        Appearance="Primary"
        Command="{Binding Path=SaveCommand, Mode=OneTime}"
        Content="{x:Static resources:Strings.CustomTheme_Editor_Save}" />

            <Button
        Grid.Column="3"
        Margin="12,0,0,0"
        MinWidth="56.587"
        Content="{x:Static resources:Strings.Common_Close}"
        IsCancel="True" />
        </Grid>

        <ui:Snackbar
      x:Name="Snackbar"
      Grid.RowSpan="3"
      Margin="200,0,200,20"
      Panel.ZIndex="9"
      Timeout="3000" />
    </Grid>
</base:WpfUiWindow>